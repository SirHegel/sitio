# Corpus verificable de @MaoteoTheDong

Corte: 28 de agosto de 2026.

Publicaciones admisibles: **0**.

Una entrada solo entra al corpus cuando reúne texto literal, fecha de publicación y enlace estable a la publicación original. Las búsquedas de esta sesión devolvieron fragmentos en agregadores y resultados sin identificador verificable; X y las instancias públicas consultadas no entregaron una fuente original estable. Ningún fragmento se usa en artículos.

Modelo de admisión:

```text
A = 1(texto literal) × 1(fecha) × 1(URL original estable)
```

La publicación entra únicamente si `A = 1`. La línea base observada es `0 / 0`; el cociente no se calcula. Umbral para iniciar inferencias estilísticas: 30 publicaciones admisibles. `σ = desconocida` porque no existe muestra.

Costo del filtro: tiempo `O(N)` y espacio `O(N)` para `N` candidatos. `TODO(dato)`: acceso a un archivo verificable del autor o URLs originales recuperables.
