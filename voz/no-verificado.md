# Material no verificado

Corte: 28 de agosto de 2026.

Las búsquedas de @MaoteoTheDong produjeron fragmentos en índices y agregadores sin una URL original estable, fecha comprobable y texto íntegro. Se excluyeron del corpus. Este archivo no reproduce los fragmentos para impedir que una cadena sin procedencia termine citada como original.

`TODO(dato)`: recuperar un archivo exportado por el titular de la cuenta o enlaces originales accesibles.

`TODO(dato)`: registrar para cada publicación `url`, `fecha UTC`, `texto literal`, `sha256(texto)` y fecha de captura.

Costo: una comprobación HTTP y una huella por candidato; tiempo `O(N)` y espacio `O(N)`. El error de cobertura permanece desconocido.
